name: Smoke

on:
  workflow_dispatch: {}

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: p6m7g8-actions/checkout@main
      - name: Run GitHub Action linter action
        uses: ./
        with:
          gh_token: ${{ secrets.GITHUB_TOKEN }}
